defmodule CommandLineFormatterTest do
  use ExUnit.Case
  import CommandLineFormatter

  describe "format_recipe/1" do
    test "takes a %Recipe{} struct and returns a string with formatted title, servings, ingredients and directions"
  end 
  end

end
